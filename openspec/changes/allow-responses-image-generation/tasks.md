## 1. Specs

- [x] 1.1 Add Responses compatibility requirements for `image_generation` tool acceptance on Responses routes.

## 2. Tests

- [x] 2.1 Add unit coverage for Responses request validation and websocket response.create normalization with `image_generation`.
- [x] 2.2 Add integration coverage for `/v1/responses` and `/backend-api/codex/responses` accepting `image_generation`.

## 3. Implementation

- [x] 3.1 Allow `image_generation` in Responses-family tool validation while keeping Chat Completions policy unchanged.
